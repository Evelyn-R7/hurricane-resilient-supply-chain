请在当前项目目录中先阅读 `START_HERE_CN.md` 和 `CODEX_INSTRUCTIONS.md`，然后把它发布为我的 GitHub 研究项目，并整合到我的 GitHub 个人主页。

具体要求：
- GitHub 用户名是 `Evelyn-R7`，先确认当前认证账号一致；
- 创建公开仓库 `Evelyn-R7/hurricane-resilient-supply-chain`；
- 不要修改、重新计算或编造研究结果；
- 不要上传 `ibtracs_NA.csv`；
- 不要恢复旧稿、废弃输出、`before/` 或 notebook checkpoint；
- 先运行 `python scripts/validate_repository.py`，验证通过后再提交；
- 使用 `main` 分支，完成初始 commit 并 push；
- 设置仓库描述和 topics，检查 README 图片、Mermaid、链接与引用是否正常；
- 仓库发布后，读取 `PROFILE_SNIPPET.md`，将项目整合到 `Evelyn-R7/Evelyn-R7` 的个人主页 README 中，但不要覆盖或删除主页原有内容；
- 不要自行添加软件许可证；
- 完成后告诉我两个仓库的链接、commit、验证结果，以及任何未完成事项。
